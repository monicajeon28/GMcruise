# 화상 회의 에러 해결 가이드

## 🔴 발생한 에러

### 1. "미팅방 생성에 실패했습니다"
**원인**: 데이터베이스 마이그레이션 미실행

**해결**: ✅ 완료
```bash
cd /home/userhyeseon28/projects/cruise-guide
npx prisma db push
```

### 2. API 500 에러
**에러 로그**:
```
/api/video/rooms?status=ACTIVE:1 Failed to load resource: the server responded with a status of 500
/api/video/rooms:1 Failed to load resource: the server responded with a status of 500
```

**원인**: 
- 데이터베이스 테이블이 없었음 (해결됨)
- Prisma 클라이언트가 업데이트되지 않았음 (해결됨)

## ✅ 해결 완료 사항

1. ✅ 데이터베이스 마이그레이션 실행 (`npx prisma db push`)
2. ✅ Prisma 클라이언트 재생성 (`npx prisma generate`)
3. ✅ API 코드 수정 (maxParticipants 기본값 수정)

## 🔍 관리자 패널에서 화상 회의 찾기

### 위치 확인

**관리자 대시보드**: `/admin/dashboard`

**화면 구성**:
```
┌─────────────────────────────────┐
│  [관리자 패널 헤더]              │
├─────────────────────────────────┤
│  📊 대시보드                     │
│  마지막 갱신: ... [갱신] 버튼    │
├─────────────────────────────────┤
│  📹 화상 회의                    │ ← 여기!
│  팀원이나 고객과 실시간...       │
│              [화상 회의] 버튼    │
├─────────────────────────────────┤
│  📊 통계 카드 (4개)              │
└─────────────────────────────────┘
```

### 화면이 안 보이는 경우

1. **페이지 새로고침** (Ctrl+Shift+R 또는 Cmd+Shift+R)
2. **브라우저 캐시 삭제**
3. **개발자 도구 확인** (F12 → Console 탭)
4. **올바른 페이지 확인**: `/admin/dashboard` (대시보드)

## 🚀 테스트 방법

### 1. 서버 재시작

```bash
cd /home/userhyeseon28/projects/cruise-guide

# 기존 서버 종료 (Ctrl+C)
# 새로 시작
npm run dev:all
```

### 2. 브라우저에서 테스트

1. **관리자 로그인**
   ```
   http://localhost:3000/admin/login
   ```

2. **대시보드 접속**
   ```
   http://localhost:3000/admin/dashboard
   ```

3. **화상 회의 섹션 확인**
   - 페이지 최상단
   - 파란색 배경 카드
   - "화상 회의" 버튼

4. **미팅 생성 테스트**
   - "화상 회의" 버튼 클릭
   - "새 미팅 시작하기" 클릭
   - 미팅 정보 입력
   - "미팅 생성" 클릭

## ⚠️ 추가 확인 사항

### Socket.io 서버 실행 확인

터미널에서 다음 메시지가 보여야 함:
```
[Socket Server] Listening on port 3001
[Socket Server] WebSocket path: /api/video/ws
[Socket Server] Ready for connections!
```

### 환경 변수 확인

`.env.local` 파일:
```bash
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
NEXT_PUBLIC_BASE_URL=http://localhost:3000
```

## 🎯 다음 단계

1. 서버 재시작
2. 브라우저 새로고침
3. 관리자 대시보드에서 화상 회의 섹션 확인
4. 미팅 생성 테스트







