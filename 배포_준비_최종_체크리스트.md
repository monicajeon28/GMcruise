# 배포 준비 최종 체크리스트

**작성일**: 2025-01-28  
**상태**: 배포 전 최종 점검

---

## ✅ 완료된 개선사항

### 1. 데이터베이스 연결 풀 설정
- [x] 코드 수정 완료
- [ ] DATABASE_URL 환경변수 업데이트 필요
  - 현재: `postgresql://...?sslmode=require`
  - 변경: `postgresql://...?sslmode=require&connection_limit=20&pool_timeout=10`

### 2. 세션 조회 Redis 캐싱
- [x] 코드 수정 완료
- [ ] Redis 환경변수 설정 필요
  - `UPSTASH_REDIS_REST_URL`
  - `UPSTASH_REDIS_REST_TOKEN`

### 3. 환율 API 캐싱
- [x] 코드 수정 완료
- [x] Redis 캐싱 적용 완료

### 4. 패키지 설치
- [x] `@upstash/redis` 설치 완료

---

## ⚠️ 발견된 에러 (수정 필요)

### 빌드 에러 (수정 완료)
- [x] `lib/push/client.ts` - 'use client' 위치 수정
- [x] `lib/csrf-client.ts` - 'use client' 위치 수정

### TypeScript 에러 (수정 필요)
- [ ] `app/admin/affiliate/agent-dashboard/page.tsx` - messageType 속성 에러 (12개)
- [ ] `app/admin/affiliate/team-dashboard/page.tsx` - messageType 속성 에러 (12개), showWarning 변수 에러
- [ ] `app/admin/landing-pages/[id]/edit/page.tsx` - 변수 선언 순서 문제
- [ ] `app/admin/landing-pages/new/page.tsx` - Date 생성자 문제
- [ ] `app/admin/marketing/customers/page.tsx` - 변수 선언 순서 문제
- [ ] `app/api/admin/affiliate/contracts/[contractId]/approve/route.ts` - Promise 타입 문제
- [ ] `app/api/admin/affiliate/contracts/[contractId]/reject/route.ts` - Promise 타입 문제
- [ ] `app/api/admin/affiliate/contracts/[contractId]/route.ts` - Promise 타입 문제
- [ ] `app/api/admin/affiliate/leads/route.ts` - 중복 속성 문제
- [ ] `app/api/admin/affiliate/profiles/[profileId]/route.ts` - Promise 타입 문제
- [ ] `app/api/admin/chat-bot/conversation/route.ts` - 타입 불일치
- [ ] `app/api/admin/insights/generate/route.ts` - PromiseSettledResult 타입 문제

**총 50개 이상의 TypeScript 에러 발견**

---

## 🚨 배포 전 필수 작업

### 1. TypeScript 에러 수정 (필수)
- [ ] 모든 TypeScript 에러 수정
- [ ] `npm run typecheck` 통과 확인

### 2. 빌드 테스트 (필수)
- [x] 빌드 에러 수정 완료
- [ ] `npm run build` 성공 확인 필요

### 3. 환경변수 설정 (필수)
- [ ] DATABASE_URL 업데이트
- [ ] UPSTASH_REDIS_REST_URL 설정
- [ ] UPSTASH_REDIS_REST_TOKEN 설정
- [ ] 기타 필수 환경변수 확인

### 4. 기능 테스트 (권장)
- [ ] 메인 페이지 접속 테스트
- [ ] 로그인 테스트
- [ ] 채팅 기능 테스트
- [ ] 주요 기능 동작 확인

---

## 📋 배포 절차

### Step 1: 에러 수정
```bash
# TypeScript 에러 확인
npm run typecheck

# 빌드 테스트
npm run build
```

### Step 2: 환경변수 설정
1. Vercel 대시보드 접속
2. Settings → Environment Variables
3. 필수 환경변수 설정

### Step 3: 배포
1. Git 커밋 및 푸시
2. Vercel 자동 배포 또는 수동 배포
3. 배포 로그 확인

### Step 4: 배포 후 테스트
1. 프로덕션 환경 접속
2. 주요 기능 테스트
3. 에러 로그 모니터링

---

## ⚡ 우선순위

### P0 - 즉시 수정 (배포 불가)
1. TypeScript 에러 수정 (50개 이상)
2. 빌드 성공 확인

### P1 - 배포 전 필수
3. 환경변수 설정
4. 기본 기능 테스트

### P2 - 배포 후 확인
5. 프로덕션 환경 테스트
6. 모니터링 설정

---

## 📝 다음 단계

1. **TypeScript 에러 수정** - 가장 시급
2. **빌드 성공 확인**
3. **환경변수 설정**
4. **기능 테스트**
5. **배포**

---

**현재 상태**: TypeScript 에러 수정 필요 (배포 불가 상태)


