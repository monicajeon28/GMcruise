# WebSocket Signaling 서버 빠른 시작 가이드

## 💡 간단 설명

**WebSocket Signaling 서버 = 완전 무료**

화상 회의에서 여러 사람이 서로 연결되려면 "누구와 연결할지" 정보를 주고받아야 합니다. 이 정보 교환을 처리하는 서버입니다.

## ✅ 비용: 0원 (완전 무료)

- 자체 서버에 구현
- 오픈소스 라이브러리 사용
- 별도 유료 서비스 불필요

## 🚀 빠른 시작 (5분)

### 1단계: 패키지 설치 (이미 완료됨)
```bash
npm install socket.io socket.io-client
```

### 2단계: 서버 실행

**터미널 1**: Next.js 개발 서버
```bash
npm run dev
```

**터미널 2**: Socket.io 서버
```bash
npm run socket
```

또는 동시에 실행:
```bash
npm run dev:all
```

### 3단계: 환경 변수 설정

`.env.local` 파일에 추가:
```bash
# Socket.io 서버 URL
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001

# 또는 프로덕션
NEXT_PUBLIC_SOCKET_URL=https://yourdomain.com:3001
```

## 📁 생성된 파일

1. **`server/socket-server.ts`**: Signaling 서버 (이미 생성됨)
2. **`components/video/VideoConference.tsx`**: Socket.io 사용으로 수정됨

## 🎯 작동 방식

1. **서버 시작**: `npm run socket` 실행
2. **클라이언트 연결**: 미팅 참가 시 자동으로 Socket.io 서버에 연결
3. **정보 교환**: 서버가 참가자들 간의 연결 정보를 전달
4. **P2P 연결**: 실제 화상 통화는 브라우저 간 직접 연결

## 🔍 확인 방법

서버가 정상 작동하면:
```
[Socket Server] Listening on port 3001
[Socket Server] WebSocket path: /api/video/ws
```

미팅 참가 시:
```
[Socket] User abc123 (홍길동) joined room xyz789
```

## 💰 비용 요약

| 항목 | 비용 |
|------|------|
| Socket.io 라이브러리 | 무료 |
| 서버 실행 | 무료 (자체 서버) |
| 유지보수 | 무료 |
| **총 비용** | **0원** |

## ⚠️ 주의사항

1. **개발 환경**: `localhost:3001`에서 실행
2. **프로덕션**: 별도 포트 또는 도메인 필요
3. **방화벽**: 포트 3001 열어야 함 (프로덕션)

## 🎉 완료!

이제 WebSocket Signaling 서버가 준비되었습니다. 추가 비용 없이 사용할 수 있습니다!







