# 🔧 AI 기능 개선 사항 상세 분석

> **작성일**: 2025년 1월 28일  
> **목적**: 경미한 개선 사항 3개에 대한 구체적 설명

---

## 1️⃣ 타임아웃 설정 추가 (우선순위: 낮음)

### 📍 현재 상태

**위치**: `app/api/chat/stream/route.ts` (236번째 줄)

**현재 코드**:
```typescript
const response = await fetch(url, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ ... }),
  cache: 'no-store',
});
// ⚠️ 타임아웃 설정 없음
```

### 🔍 문제점

1. **무한 대기 가능성**
   - Gemini API가 응답하지 않으면 계속 대기
   - 네트워크 문제 시 사용자가 오래 기다림
   - 서버 리소스 점유 시간 증가

2. **사용자 경험 저하**
   - 응답이 없을 때 언제까지 기다려야 할지 모름
   - 브라우저가 타임아웃할 때까지 대기 (보통 2-5분)

3. **서버 리소스 낭비**
   - 응답이 없는 요청이 계속 메모리에 남아있음
   - 동시 요청 수 제한에 영향

### 💡 해결 방법

**AbortController를 사용한 타임아웃 설정**:

```typescript
// 타임아웃 설정 (예: 60초)
const timeoutMs = 60000; // 60초
const abortController = new AbortController();
const timeoutId = setTimeout(() => {
  abortController.abort();
}, timeoutMs);

try {
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ ... }),
    cache: 'no-store',
    signal: abortController.signal, // 타임아웃 신호 추가
  });
  
  clearTimeout(timeoutId); // 성공 시 타임아웃 취소
  
  // ... 나머지 코드
} catch (error: any) {
  clearTimeout(timeoutId);
  if (error.name === 'AbortError') {
    // 타임아웃 에러 처리
    return new Response(JSON.stringify({ 
      error: '요청 시간이 초과되었습니다. 잠시 후 다시 시도해주세요.' 
    }), { 
      status: 408,
      headers: { 'Content-Type': 'application/json' }
    });
  }
  // 기타 에러 처리
}
```

### 📊 영향도 분석

| 항목 | 현재 | 개선 후 |
|------|------|---------|
| 최대 대기 시간 | 무제한 (2-5분) | 60초 |
| 사용자 경험 | 불명확한 대기 | 명확한 타임아웃 메시지 |
| 서버 리소스 | 낭비 가능 | 효율적 관리 |
| **실제 발생 빈도** | **매우 낮음** (Gemini API는 보통 빠름) |

### ⏱️ 예상 작업 시간

- 코드 수정: 20분
- 테스트: 10분
- **총 예상 시간**: 30분

### 🎯 결론

- **필수 여부**: 선택사항
- **우선순위**: 낮음
- **이유**: Gemini API는 일반적으로 빠르게 응답하므로 실제로 타임아웃이 발생할 가능성이 매우 낮음

---

## 2️⃣ 대량 문서 검색 최적화 (우선순위: 낮음)

### 📍 현재 상태

**위치**: `lib/ai/ragSearch.ts` (69-82번째 줄)

**현재 코드**:
```typescript
// 2. KnowledgeBase에서 활성화된 문서 가져오기 (임베딩 포함)
const knowledgeItems = await prisma.knowledgeBase.findMany({
  where: {
    isActive: true,
    embedding: { not: null },
  },
  select: {
    id: true,
    title: true,
    content: true,
    category: true,
    keywords: true,
    embedding: true, // 모든 문서의 임베딩을 메모리에 로드
  },
});

// 3. 각 문서와의 유사도 계산 (메모리에서)
for (const item of knowledgeItems) {
  const similarity = cosineSimilarity(normalizedQueryEmbedding, normalizedItemEmbedding);
  // ...
}
```

### 🔍 문제점

1. **메모리 사용량 증가**
   - 모든 문서의 임베딩을 메모리에 로드
   - 임베딩 하나당 약 3KB (768차원 × 4바이트)
   - 문서 1,000개 = 약 3MB 메모리 사용
   - 문서 10,000개 = 약 30MB 메모리 사용

2. **처리 시간 증가**
   - 모든 문서와 유사도 계산 (O(n))
   - 문서가 많을수록 선형적으로 증가
   - 예: 1,000개 문서 = 1,000번 계산
   - 예: 10,000개 문서 = 10,000번 계산

3. **확장성 문제**
   - 문서가 많아질수록 성능 저하
   - 벡터 DB를 사용하면 더 효율적

### 💡 해결 방법

**옵션 1: 벡터 DB 사용 (pgvector)** (권장)

```typescript
// PostgreSQL + pgvector 확장 사용
// Prisma 스키마에 벡터 타입 추가
model KnowledgeBase {
  id        Int      @id @default(autoincrement())
  title     String
  content   String
  embedding Unsupported("vector(768)")? // pgvector 타입
  // ...
}

// 유사도 검색을 DB에서 처리
const results = await prisma.$queryRaw`
  SELECT 
    id, title, content, category,
    1 - (embedding <=> ${queryEmbedding}::vector) as similarity
  FROM "KnowledgeBase"
  WHERE is_active = true 
    AND embedding IS NOT NULL
  ORDER BY embedding <=> ${queryEmbedding}::vector
  LIMIT ${limit}
`;
```

**옵션 2: 배치 처리** (간단한 개선)

```typescript
// 문서를 배치로 나누어 처리
const BATCH_SIZE = 100;
const batches = [];
for (let i = 0; i < knowledgeItems.length; i += BATCH_SIZE) {
  batches.push(knowledgeItems.slice(i, i + BATCH_SIZE));
}

// 배치별로 처리 (메모리 사용량 감소)
for (const batch of batches) {
  for (const item of batch) {
    // 유사도 계산
  }
}
```

### 📊 영향도 분석

| 항목 | 현재 | 개선 후 (pgvector) |
|------|------|-------------------|
| 메모리 사용 | O(n) - 모든 문서 로드 | O(1) - DB에서 처리 |
| 처리 시간 | O(n) - 모든 문서 계산 | O(log n) - 인덱스 사용 |
| 확장성 | 문서 증가 시 선형 저하 | 문서 증가 시에도 안정적 |
| **현재 문서 수** | **예상: 100-500개** (충분히 빠름) |

### ⏱️ 예상 작업 시간

**옵션 1 (pgvector)**:
- pgvector 설치 및 설정: 2시간
- Prisma 스키마 수정: 1시간
- 코드 수정: 2시간
- 테스트: 1시간
- **총 예상 시간**: 6시간

**옵션 2 (배치 처리)**:
- 코드 수정: 1시간
- 테스트: 30분
- **총 예상 시간**: 1.5시간

### 🎯 결론

- **필수 여부**: 선택사항
- **우선순위**: 낮음
- **이유**: 현재 문서 수가 적어서 성능 문제가 발생하지 않음. 문서가 1,000개 이상이 되면 고려

---

## 3️⃣ 임베딩 차원 통일 (우선순위: 매우 낮음)

### 📍 현재 상태

**위치**: `lib/ai/embeddingUtils.ts`

**현재 코드**:
```typescript
// Google Embedding API 사용 시
const modelName = 'text-embedding-004';
// 실제 차원: 확인 필요 (보통 768 또는 다른 값)

// 폴백: 해시 기반 임베딩
function generateSimpleEmbedding(text: string): number[] {
  // 간단한 해시 기반 벡터 생성 (768차원)
  const vector: number[] = [];
  // ...
  for (let i = 0; i < 768; i++) { // ⚠️ 하드코딩된 768차원
    // ...
  }
  return vector;
}
```

### 🔍 문제점

1. **차원 불일치 가능성**
   - Google Embedding API (`text-embedding-004`)의 실제 차원이 768이 아닐 수 있음
   - 폴백 임베딩은 항상 768차원
   - 실제 API는 다른 차원일 수 있음

2. **유사도 계산 오류**
   - 차원이 다르면 코사인 유사도 계산 실패
   - `cosineSimilarity()` 함수에서 에러 발생
   - 현재는 에러 처리로 넘어가지만 정확도 저하

3. **혼란 가능성**
   - 개발자가 차원을 잘못 이해할 수 있음
   - 문서화 부족

### 💡 해결 방법

**옵션 1: 실제 API 차원 확인 후 통일** (권장)

```typescript
// 1. Google Embedding API의 실제 차원 확인
// text-embedding-004는 보통 768차원이지만 확인 필요

// 2. 상수로 정의
const EMBEDDING_DIMENSION = 768; // 또는 실제 API 차원

// 3. 폴백 임베딩도 동일 차원 사용
function generateSimpleEmbedding(text: string): number[] {
  const vector: number[] = [];
  const hash = simpleHash(text);
  
  for (let i = 0; i < EMBEDDING_DIMENSION; i++) { // 상수 사용
    const seed = hash + i;
    vector.push(Math.sin(seed) * 0.5 + 0.5);
  }
  
  return vector;
}

// 4. 차원 검증 추가
export async function generateEmbedding(text: string): Promise<number[]> {
  // ... API 호출
  
  const embedding = data.embedding?.values;
  
  if (!embedding || !Array.isArray(embedding)) {
    throw new Error('임베딩 응답 형식이 올바르지 않습니다.');
  }
  
  // 차원 검증
  if (embedding.length !== EMBEDDING_DIMENSION) {
    console.warn(`[Embedding] 차원 불일치: 예상 ${EMBEDDING_DIMENSION}, 실제 ${embedding.length}`);
    // 또는 EMBEDDING_DIMENSION을 동적으로 업데이트
  }
  
  return embedding;
}
```

**옵션 2: 동적 차원 감지**

```typescript
// 첫 번째 API 호출 시 차원을 감지하고 저장
let detectedDimension: number | null = null;

export async function generateEmbedding(text: string): Promise<number[]> {
  // ... API 호출
  
  const embedding = data.embedding?.values;
  
  if (!detectedDimension && embedding) {
    detectedDimension = embedding.length;
    console.log(`[Embedding] 감지된 차원: ${detectedDimension}`);
  }
  
  return embedding;
}

// 폴백 임베딩도 동적 차원 사용
function generateSimpleEmbedding(text: string): number[] {
  const dimension = detectedDimension || 768; // 기본값 768
  const vector: number[] = [];
  // ...
  for (let i = 0; i < dimension; i++) {
    // ...
  }
  return vector;
}
```

### 📊 영향도 분석

| 항목 | 현재 | 개선 후 |
|------|------|---------|
| 차원 일치 | 불확실 | 확실 |
| 유사도 계산 | 에러 처리로 넘어감 | 정확한 계산 |
| 정확도 | 약간 저하 가능 | 정확 |
| **실제 발생 빈도** | **매우 낮음** (폴백 사용 시에만) |

### ⏱️ 예상 작업 시간

- Google API 차원 확인: 10분
- 코드 수정: 30분
- 테스트: 20분
- **총 예상 시간**: 1시간

### 🎯 결론

- **필수 여부**: 선택사항
- **우선순위**: 매우 낮음
- **이유**: 
  - 폴백 임베딩은 API 키가 없을 때만 사용 (개발 환경)
  - 실제 배포 환경에서는 Google Embedding API를 사용하므로 문제 없음
  - 차원 불일치가 발생해도 에러 처리로 넘어가므로 치명적이지 않음

---

## 📊 종합 비교표

| 개선 사항 | 현재 영향 | 개선 후 효과 | 작업 시간 | 우선순위 |
|----------|---------|------------|----------|---------|
| 타임아웃 설정 | 매우 낮음 | 사용자 경험 개선 | 30분 | 낮음 |
| 대량 문서 최적화 | 낮음 | 확장성 향상 | 6시간 | 낮음 |
| 임베딩 차원 통일 | 매우 낮음 | 정확도 향상 | 1시간 | 매우 낮음 |

---

## 🎯 최종 권장사항

### ✅ **배포 전 필수 작업 아님**

모든 개선 사항은 **선택사항**입니다.

**이유**:
1. **타임아웃 설정**: Gemini API는 일반적으로 빠르게 응답하므로 실제 문제 발생 가능성 매우 낮음
2. **대량 문서 최적화**: 현재 문서 수가 적어서 성능 문제 없음
3. **임베딩 차원 통일**: 배포 환경에서는 Google Embedding API를 사용하므로 문제 없음

### 📅 **개선 시기**

- **배포 후**: 사용자 피드백을 받고 필요 시 개선
- **문서 수 증가 시**: 대량 문서 최적화 고려
- **성능 문제 발생 시**: 타임아웃 설정 추가

---

**작성일**: 2025년 1월 28일  
**결론**: 모든 개선 사항은 선택사항이며, 현재 상태로도 배포 가능


