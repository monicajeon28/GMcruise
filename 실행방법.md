# 화상 회의 시스템 실행 방법

## ⚠️ 중요: 올바른 디렉토리에서 실행하세요!

현재 `/home/userhyeseon28/projects` 디렉토리에 있으면 안 됩니다.
**반드시 `/home/userhyeseon28/projects/cruise-guide` 디렉토리로 이동**해야 합니다.

## 🚀 실행 방법

### 방법 1: 올바른 디렉토리로 이동 후 실행

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/userhyeseon28/projects/cruise-guide

# 2. Next.js 서버 실행 (터미널 1)
npm run dev

# 3. Socket.io 서버 실행 (터미널 2 - 새 터미널 열기)
cd /home/userhyeseon28/projects/cruise-guide
npm run socket
```

### 방법 2: 한 번에 실행 (권장)

```bash
# 프로젝트 디렉토리로 이동
cd /home/userhyeseon28/projects/cruise-guide

# Next.js + Socket.io 동시 실행
npm run dev:all
```

## ✅ 정상 작동 확인

### Next.js 서버
```
▲ Next.js 14.2.3
- Local:        http://localhost:3000
```

### Socket.io 서버
```
[Socket Server] Listening on port 3001
[Socket Server] WebSocket path: /api/video/ws
```

## 🔧 문제 해결

### 에러: "Could not read package.json"
**원인**: 잘못된 디렉토리에서 실행
**해결**: 
```bash
cd /home/userhyeseon28/projects/cruise-guide
```

### 에러: "Cannot find module"
**원인**: 패키지 미설치
**해결**:
```bash
cd /home/userhyeseon28/projects/cruise-guide
npm install
```

### Socket.io 서버가 시작되지 않음
**해결**:
```bash
cd /home/userhyeseon28/projects/cruise-guide
npm install socket.io @types/node
npm run socket
```

## 📝 환경 변수 설정

`.env.local` 파일에 추가:
```bash
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
NEXT_PUBLIC_BASE_URL=http://localhost:3000
```

## 🎯 빠른 체크리스트

- [ ] `/home/userhyeseon28/projects/cruise-guide` 디렉토리에 있는지 확인
- [ ] `npm install` 실행 완료
- [ ] `.env.local` 파일 설정 완료
- [ ] `npm run dev` 실행 (또는 `npm run dev:all`)
- [ ] 두 서버 모두 정상 작동 확인







