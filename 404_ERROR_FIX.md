# 크루즈몰 404 에러 해결 가이드

> **작성일**: 2025년 11월 20일  
> **문제**: 브라우저에서 Next.js 빌드 파일 404 에러

---

## 🔍 문제 분석

### 발생한 에러

```
Failed to load resource: the server responded with a status of 404 (Not Found)
- app-pages-internals.js: 404
- main-app.js: 404
- page.js: 404
- layout.css: 404
```

**원인**:
- Next.js 빌드 산출물이 손상되었거나 캐시 문제
- 개발 서버가 빌드 파일을 제대로 서빙하지 못함

---

## ✅ 해결 방법

### 방법 1: 캐시 삭제 및 재시작 (권장)

```bash
# 1. 프로젝트 디렉토리로 이동
cd ~/projects/cruise-guide

# 2. .next 디렉토리 삭제 (빌드 캐시 삭제)
rm -rf .next

# 3. 개발 서버 종료 (실행 중인 경우)
pkill -f "next dev"

# 4. 개발 서버 재시작
npm run dev
```

### 방법 2: 완전 재빌드

```bash
# 1. 프로젝트 디렉토리로 이동
cd ~/projects/cruise-guide

# 2. 모든 캐시 삭제
rm -rf .next
rm -rf node_modules/.cache

# 3. 개발 서버 재시작
npm run dev
```

### 방법 3: 포트 확인 및 변경

```bash
# 다른 포트로 실행
PORT=3001 npm run dev
```

---

## 🔧 추가 확인 사항

### 1. 포트 확인

개발 서버가 올바른 포트에서 실행 중인지 확인:

```bash
# 포트 확인
lsof -i :3000

# 또는
netstat -tulpn | grep 3000
```

### 2. 브라우저 캐시 클리어

브라우저에서:
- `Ctrl + Shift + R` (하드 리프레시)
- 또는 개발자 도구 → Network 탭 → "Disable cache" 체크

### 3. 환경 변수 확인

```bash
# .env.local 파일 확인
cat .env.local | grep -E "PORT|NEXT_PUBLIC"
```

---

## 📝 예방 방법

### 정기적인 캐시 정리

개발 중 문제가 발생하면:

```bash
# 빠른 캐시 정리
rm -rf .next && npm run dev
```

### 빌드 문제 해결

```bash
# 완전 재빌드
rm -rf .next node_modules/.cache
npm run build
npm run dev
```

---

## 🚨 여전히 문제가 있는 경우

### 1. Node.js 버전 확인

```bash
node --version
# Next.js 14는 Node.js 18.17 이상 필요
```

### 2. 의존성 재설치

```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### 3. Next.js 버전 확인

```bash
npm list next
```

---

## ✅ 해결 확인

해결 후 다음을 확인:

1. 브라우저 콘솔에 에러가 없는지 확인
2. 페이지가 정상적으로 로드되는지 확인
3. 네트워크 탭에서 모든 리소스가 200 상태로 로드되는지 확인

---

**작성자**: AI Assistant  
**최종 업데이트**: 2025년 11월 20일


