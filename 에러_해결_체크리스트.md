# API 500 에러 해결 체크리스트

## 🔍 단계별 확인

### 1단계: 서버 로그 확인

**Next.js 서버 터미널에서 다음 로그를 확인하세요:**

#### 미팅 생성 시 (POST):
```
[Video Rooms] Creating meeting room: { roomId: ..., hostId: ..., ... }
[Video Rooms] Meeting room created successfully: ...
```

또는 에러:
```
[Video Rooms] Create database error: ...
[Video Rooms] Error details: { message: ..., code: ..., meta: ... }
```

#### 미팅 목록 조회 시 (GET):
```
[Video Rooms] Found X rooms with status: ACTIVE
[Video Rooms] Successfully fetched X rooms
```

또는 에러:
```
[Video Rooms] Database query error: ...
[Video Rooms] Error details: { message: ..., code: ..., meta: ... }
```

### 2단계: 브라우저 Network 탭 확인

1. F12 → Network 탭
2. `/api/video/rooms` 요청 클릭
3. **Response** 탭에서 에러 메시지 확인
4. `details` 필드에 상세 에러 정보가 있습니다

### 3단계: 일반적인 문제 해결

#### 문제 1: "세션 조회에 실패했습니다"
**해결**:
```bash
# 서버 재시작
cd /home/userhyeseon28/projects/cruise-guide
npm run dev:all
```

#### 문제 2: "인증이 필요합니다"
**해결**:
1. 로그아웃 후 다시 로그인
2. 브라우저 쿠키 확인 (F12 → Application → Cookies)

#### 문제 3: 데이터베이스 에러
**해결**:
```bash
cd /home/userhyeseon28/projects/cruise-guide
npx prisma db push
npx prisma generate
```

### 4단계: 에러 메시지 공유

서버 터미널의 **전체 에러 로그**를 복사해서 알려주세요.

특히 다음 정보가 필요합니다:
- `[Video Rooms] Error details:` 뒤의 전체 내용
- `message`, `code`, `meta` 필드 값

## 📝 추가된 로깅

1. ✅ 미팅 생성 전 로그
2. ✅ 미팅 생성 성공 로그
3. ✅ 미팅 목록 조회 전 카운트 로그
4. ✅ 미팅 목록 조회 성공 로그
5. ✅ 상세 에러 정보 로깅

## 🎯 다음 단계

1. 서버 재시작
2. 화상 회의 기능 테스트
3. 서버 터미널 로그 확인
4. 에러 로그를 알려주시면 정확한 해결책 제시







