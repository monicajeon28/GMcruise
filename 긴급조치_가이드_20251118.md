# 🚨 크루즈가이드 프로젝트 - 긴급 조치 가이드

> **작성일**: 2025년 11월 18일
> **긴급도**: 🔴 Critical
> **예상 작업 시간**: 3-4시간

---

## ⚠️ 즉시 조치 필요 사항

이 가이드는 **배포 전 반드시 해결해야 할 Critical 보안 이슈**들을 정리한 것입니다.

---

## 1️⃣ .env 파일 Git 추적 제거 (30분)

### 현재 상태
```bash
$ git ls-files | grep -E "^\.env$"
.env  # ← Git에 추적되고 있음!
```

### 위험도
🔴 **Critical** - 모든 비밀 키와 환경 변수가 노출될 수 있음

### 즉시 조치
```bash
cd /home/userhyeseon28/projects/cruise-guide

# 1. Git 추적 제거
git rm --cached .env
git rm --cached .env.local 2>/dev/null || true

# 2. .gitignore에 추가
cat >> .gitignore << 'EOF'

# 환경 변수 파일
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# .env.example은 추적 (템플릿)
!.env.example
EOF

# 3. 커밋
git add .gitignore
git commit -m "security: Remove .env files from Git tracking

- Remove .env and .env.local from Git
- Update .gitignore to prevent future tracking
- CRITICAL: All API keys need to be rotated if already pushed to remote"

# 4. 확인
git status
git ls-files | grep -E "^\.env$" || echo "✅ .env successfully removed from Git"
```

### GitHub/GitLab에 이미 푸시된 경우

**⚠️ 모든 비밀 키를 즉시 회전해야 합니다!**

```bash
# Git 히스토리에서 완전히 제거
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env .env.local" \
  --prune-empty --tag-name-filter cat -- --all

# 강제 푸시 (주의: 협업 중이면 팀원과 조율)
git push origin --force --all
git push origin --force --tags
```

### 비밀 키 회전 체크리스트

- [ ] `DATABASE_URL` 비밀번호 변경
- [ ] `SESSION_SECRET` 재생성 (`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)
- [ ] `GOOGLE_API_KEY` 재발급
- [ ] `KAKAO_REST_API_KEY` 재발급
- [ ] `KAKAO_ADMIN_KEY` 재발급
- [ ] `GEMINI_API_KEY` 재발급
- [ ] `EMAIL_SMTP_PASSWORD` 변경

---

## 2️⃣ 하드코딩된 API 키 제거 (1시간)

### 발견된 하드코딩

#### `/app/api/admin/settings/info/route.ts`
```typescript
// 97-98번째 줄
kakaoRestApiKey: process.env.KAKAO_REST_API_KEY || 'e75220229cf63f62a0832447850985ea',
kakaoAdminKey: process.env.KAKAO_ADMIN_KEY || '6f2872dfa8ac40ab0d9a93a70c542d10',
```

### 조치 방법

```bash
# 파일 열기
code app/api/admin/settings/info/route.ts
```

**수정 전**:
```typescript
kakaoRestApiKey: process.env.KAKAO_REST_API_KEY || 'e75220229cf63f62a0832447850985ea',
kakaoAdminKey: process.env.KAKAO_ADMIN_KEY || '6f2872dfa8ac40ab0d9a93a70c542d10',
```

**수정 후**:
```typescript
kakaoRestApiKey: process.env.KAKAO_REST_API_KEY || '',
kakaoAdminKey: process.env.KAKAO_ADMIN_KEY || '',
```

또는 더 안전하게:
```typescript
// 환경 변수 없으면 에러 발생
if (!process.env.KAKAO_REST_API_KEY || !process.env.KAKAO_ADMIN_KEY) {
  return NextResponse.json(
    { ok: false, error: 'Kakao API keys not configured' },
    { status: 500 }
  );
}

const info = {
  // ...
  kakaoRestApiKey: process.env.KAKAO_REST_API_KEY,
  kakaoAdminKey: process.env.KAKAO_ADMIN_KEY,
  // ...
};
```

### Kakao API 키 회전

1. **Kakao Developers 접속**: https://developers.kakao.com
2. **내 애플리케이션** → 해당 앱 선택
3. **앱 키** 섹션:
   - REST API 키 재발급
   - Admin 키 재발급
4. **기존 키 비활성화** (가능한 경우)
5. **.env에 새 키 설정**:
```env
KAKAO_REST_API_KEY="새로_발급받은_키"
KAKAO_ADMIN_KEY="새로_발급받은_키"
```

---

## 3️⃣ 테스트/관리자 기본 비밀번호 비활성화 (1시간)

### 발견된 하드코딩

#### `/app/api/auth/login/route.ts`
```typescript
// 14번째 줄
const TEST_MODE_PASSWORDS = ['1101', '0313'];

// 938-980번째 줄
if (password === '0313') {
  isPasswordValid = true; // 무조건 허용!
}
```

### 조치 방법

```bash
# 파일 열기
code app/api/auth/login/route.ts
```

**수정 전**:
```typescript
const TEST_MODE_PASSWORDS = ['1101', '0313'];
```

**수정 후**:
```typescript
const isDevelopment = process.env.NODE_ENV === 'development';
const TEST_MODE_PASSWORDS = isDevelopment
  ? (process.env.TEST_PASSWORDS?.split(',') || [])
  : [];
```

**.env.local (개발 환경만)**:
```env
NODE_ENV="development"
TEST_PASSWORDS="1101,0313"
```

**.env.production**:
```env
NODE_ENV="production"
# TEST_PASSWORDS 설정하지 않음 (프로덕션에서 비활성화)
```

**관리자 기본 비밀번호 제거**:
```typescript
// 수정 전
if (password === '0313') {
  isPasswordValid = true;
}

// 수정 후 - 완전히 제거
// (초기 관리자 계정은 별도 스크립트로 생성)
```

---

## 4️⃣ WebSocket 인증 추가 (2-3시간)

### 현재 상태

#### `/server/socket-server.ts`
```typescript
io.on('connection', (socket) => {
  const { roomId, userId, userName, isHost } = socket.handshake.query;
  // 세션 검증 없음! 누구나 임의 userId로 접속 가능
});
```

### 조치 방법

#### 1. 세션 검증 함수 추가

```typescript
// server/socket-server.ts 상단에 추가
import { getIronSession } from 'iron-session';
import { sessionOptions } from '../lib/session';

async function verifySocketSession(socket: any) {
  try {
    const cookie = socket.handshake.headers.cookie;
    if (!cookie) return null;

    // Cookie 파싱
    const cookies = cookie.split(';').reduce((acc: any, curr: string) => {
      const [key, value] = curr.trim().split('=');
      acc[key] = value;
      return acc;
    }, {});

    const sessionCookie = cookies['cg.sid.v2'];
    if (!sessionCookie) return null;

    // 세션 검증 (실제 구현은 프로젝트 구조에 맞게 조정)
    // Prisma로 세션 조회
    const session = await prisma.session.findUnique({
      where: { sessionToken: sessionCookie },
      include: { user: true },
    });

    if (!session || session.expires < new Date()) {
      return null;
    }

    return session.user;
  } catch (error) {
    console.error('Socket session verification failed:', error);
    return null;
  }
}
```

#### 2. 연결 시 검증

```typescript
io.on('connection', async (socket) => {
  // 세션 검증
  const user = await verifySocketSession(socket);
  if (!user) {
    socket.emit('error', { message: 'Unauthorized' });
    socket.disconnect();
    return;
  }

  const { roomId } = socket.handshake.query;
  if (!roomId) {
    socket.disconnect();
    return;
  }

  // 검증된 사용자 정보 사용
  const userId = user.id.toString();
  const userName = user.displayName;

  // 나머지 로직...
});
```

#### 3. 호스트 권한 서버에서 관리

```typescript
// DB에 방 정보 저장
const room = await prisma.meetingRoom.upsert({
  where: { meetingLink: roomId as string },
  update: {
    // 업데이트
  },
  create: {
    meetingLink: roomId as string,
    hostUserId: user.id,
    status: 'active',
  },
});

// 호스트인지 확인
const isHost = room.hostUserId === user.id;
```

---

## 5️⃣ CORS 와일드카드 제거 (30분)

### 현재 상태

#### `/server/socket-server.ts`
```typescript
cors: {
  origin: process.env.NEXT_PUBLIC_BASE_URL || '*',  // 위험!
}
```

### 조치 방법

```typescript
// server/socket-server.ts 수정
const allowedOrigins = [
  'http://localhost:3030',
  'http://localhost:3000',
  process.env.NEXT_PUBLIC_BASE_URL,
].filter(Boolean);

const io = new Server(httpServer, {
  cors: {
    origin: (origin, callback) => {
      // origin이 없으면 (동일 출처) 허용
      if (!origin) return callback(null, true);

      if (allowedOrigins.includes(origin)) {
        callback(null, true);
      } else {
        callback(new Error('Not allowed by CORS'));
      }
    },
    methods: ['GET', 'POST'],
    credentials: true,
  },
});

// 프로덕션 환경에서 명시적 도메인 필수
if (process.env.NODE_ENV === 'production' && !process.env.NEXT_PUBLIC_BASE_URL) {
  console.error('⚠️  NEXT_PUBLIC_BASE_URL is required in production');
  process.exit(1);
}
```

**.env.production**:
```env
NEXT_PUBLIC_BASE_URL="https://your-production-domain.com"
```

---

## 6️⃣ Admin API 비밀 정보 마스킹 (1-2시간)

### 현재 상태

#### `/app/api/admin/settings/info/route.ts`
```typescript
const info = {
  emailSmtpPassword: process.env.EMAIL_SMTP_PASSWORD || '',
  geminiApiKey: process.env.GEMINI_API_KEY || '',
  // ... 모든 비밀 키 노출
};
return NextResponse.json({ ok: true, info });
```

### 조치 방법

#### 1. 마스킹 함수 추가

```typescript
// app/api/admin/settings/info/route.ts 상단에 추가
function maskSecret(secret: string): string {
  if (!secret) return '';
  if (secret.length <= 8) return '***';
  return `${secret.slice(0, 4)}${'*'.repeat(Math.max(4, secret.length - 8))}${secret.slice(-4)}`;
}
```

#### 2. 모든 비밀 정보 마스킹

```typescript
const info = {
  // 이메일 설정
  emailSmtpHost: process.env.EMAIL_SMTP_HOST || '',
  emailSmtpPort: process.env.EMAIL_SMTP_PORT || '587',
  emailSmtpUser: process.env.EMAIL_SMTP_USER || '',
  emailSmtpPassword: maskSecret(process.env.EMAIL_SMTP_PASSWORD || ''),  // 마스킹
  emailFromAddress: process.env.EMAIL_FROM_ADDRESS || '',

  // API 키
  geminiApiKey: maskSecret(process.env.GEMINI_API_KEY || ''),  // 마스킹
  kakaoRestApiKey: maskSecret(process.env.KAKAO_REST_API_KEY || ''),  // 마스킹
  kakaoAdminKey: maskSecret(process.env.KAKAO_ADMIN_KEY || ''),  // 마스킹

  // 데이터베이스
  databaseUrl: maskSecret(process.env.DATABASE_URL || ''),  // 마스킹

  // 세션
  sessionSecret: maskSecret(process.env.SESSION_SECRET || ''),  // 마스킹

  // 기타
  nextPublicBaseUrl: process.env.NEXT_PUBLIC_BASE_URL || '',  // 공개 정보는 마스킹 안 함
};
```

#### 3. 업데이트 API 분리

```typescript
// 설정 조회 (마스킹된 값 반환)
export async function GET(request: NextRequest) {
  // 위 코드
}

// 설정 업데이트 (실제 값 저장, 마스킹 값 무시)
export async function POST(request: NextRequest) {
  const body = await request.json();

  // 마스킹된 값은 무시
  const updates: Record<string, string> = {};
  for (const [key, value] of Object.entries(body)) {
    if (typeof value === 'string' && !value.includes('*')) {
      updates[key] = value;
    }
  }

  // .env 파일 업데이트 또는 DB 저장
  // ...
}
```

---

## 7️⃣ 채팅 히스토리 저장 기능 완성 (2-3시간)

### 현재 상태

- `/app/chat/history/route.ts` - import 오류
- `ChatClientShell.tsx` - 저장 로직 없음

### 조치 방법

#### 1. import 오류 수정

```bash
code app/chat/history/route.ts
```

**수정 전**:
```typescript
import { authOptions } from '@/lib/auth';  // 잘못된 import
import { prisma } from '@/lib/prisma';  // 잘못된 import
```

**수정 후**:
```typescript
import { getSessionUser } from '@/lib/session';  // 올바른 import
import prisma from '@/lib/prisma';  // default export 사용
```

#### 2. 저장 API 구현

```typescript
// app/chat/history/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getSessionUser } from '@/lib/session';
import prisma from '@/lib/prisma';

// 조회
export async function GET(request: NextRequest) {
  try {
    const user = await getSessionUser();
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const history = await prisma.chatHistory.findMany({
      where: { userId: user.id },
      orderBy: { createdAt: 'desc' },
      take: 100,
    });

    return NextResponse.json({ ok: true, history });
  } catch (error) {
    console.error('Failed to fetch chat history:', error);
    return NextResponse.json({ error: 'Failed to fetch history' }, { status: 500 });
  }
}

// 저장
export async function POST(request: NextRequest) {
  try {
    const user = await getSessionUser();
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const { messages } = await request.json();

    if (!Array.isArray(messages) || messages.length === 0) {
      return NextResponse.json({ error: 'Invalid messages' }, { status: 400 });
    }

    // 최근 메시지만 저장 (마지막 대화)
    const lastMessage = messages[messages.length - 1];

    await prisma.chatHistory.create({
      data: {
        userId: user.id,
        role: lastMessage.role,
        content: lastMessage.content,
        metadata: lastMessage.metadata || {},
      },
    });

    return NextResponse.json({ ok: true });
  } catch (error) {
    console.error('Failed to save chat history:', error);
    return NextResponse.json({ error: 'Failed to save history' }, { status: 500 });
  }
}
```

#### 3. ChatClientShell.tsx에 저장 로직 추가

```bash
code app/chat/components/ChatClientShell.tsx
```

```typescript
import { useEffect } from 'react';

// 컴포넌트 내부
useEffect(() => {
  // 메시지가 추가될 때마다 저장 (디바운스)
  if (messages.length > 0) {
    const timeoutId = setTimeout(() => {
      fetch('/api/chat/history', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ messages }),
      }).catch((error) => {
        console.error('Failed to save chat history:', error);
      });
    }, 1000); // 1초 디바운스

    return () => clearTimeout(timeoutId);
  }
}, [messages]);

// 컴포넌트 마운트 시 히스토리 복원
useEffect(() => {
  fetch('/api/chat/history')
    .then((res) => res.json())
    .then((data) => {
      if (data.ok && data.history) {
        // 히스토리를 메시지 형식으로 변환
        const restored = data.history.map((item: any) => ({
          role: item.role,
          content: item.content,
          metadata: item.metadata,
        }));
        setMessages(restored);
      }
    })
    .catch((error) => {
      console.error('Failed to restore chat history:', error);
    });
}, []);
```

---

## ✅ 완료 체크리스트

작업을 완료하면 체크하세요:

- [ ] 1. .env 파일 Git 추적 제거 완료
- [ ] 2. 하드코딩된 API 키 제거 완료
- [ ] 3. 테스트 비밀번호 환경 변수화 완료
- [ ] 4. WebSocket 세션 인증 추가 완료
- [ ] 5. CORS 와일드카드 제거 완료
- [ ] 6. Admin API 비밀 정보 마스킹 완료
- [ ] 7. 채팅 히스토리 저장 기능 완성

**모두 완료 후**:
```bash
# 빌드 테스트
npm run build

# 타입 체크
npx tsc --noEmit

# Git 커밋
git add .
git commit -m "security: Fix critical security issues

- Remove .env from Git tracking
- Remove hardcoded API keys
- Disable test passwords in production
- Add WebSocket authentication
- Fix CORS wildcard
- Mask sensitive data in Admin API
- Complete chat history save feature"

# 배포 (모든 테스트 통과 후)
vercel --prod
```

---

## 🆘 문제 발생 시

### Git 관련 오류
```bash
# .env 제거 실패 시
git rm -f --cached .env

# 강제 푸시 실패 시
git reflog  # 복구 가능
```

### 빌드 오류
```bash
# 캐시 삭제 후 재빌드
rm -rf .next
npm run build
```

### 세션 인증 오류
```bash
# Prisma 클라이언트 재생성
npx prisma generate
```

---

**이 가이드를 따라 모든 조치를 완료한 후 배포를 진행하세요.**
**완료 예상 시간: 3-4시간**