# 📦 GitHub 저장소 만들기 완전 가이드

**초보자를 위한 단계별 가이드**

---

## 🎯 1단계: GitHub 접속 및 로그인

### 1-1. GitHub 웹사이트 접속
1. 브라우저를 엽니다 (Chrome, Edge, Firefox 등)
2. 주소창에 입력: `https://github.com`
3. Enter 키를 누릅니다

### 1-2. 로그인
- **이미 계정이 있으면**: 로그인
- **계정이 없으면**: 
  1. "Sign up" 버튼 클릭
  2. 이메일, 비밀번호 입력
  3. 사용자명 입력
  4. 회원가입 완료

---

## 🎯 2단계: 새 저장소 만들기

### 2-1. 저장소 생성 버튼 찾기
1. GitHub 메인 페이지에서
2. **우측 상단**을 봅니다
3. **"+"** 버튼을 찾습니다 (플러스 아이콘)
4. **"+"** 버튼을 클릭합니다

### 2-2. "New repository" 선택
- 드롭다운 메뉴가 나타납니다
- **"New repository"**를 클릭합니다

---

## 🎯 3단계: 저장소 정보 입력

### 3-1. Repository name (저장소 이름)
- 입력란에 입력: `cruise-guide`
- 또는 원하는 이름 입력 (예: `my-cruise-app`)

### 3-2. Description (설명) - 선택사항
- 입력란에 입력: `크루즈 가이드 관리 시스템`
- 또는 비워둬도 됩니다

### 3-3. Public / Private 선택
- **Public**: 누구나 볼 수 있음 (무료)
- **Private**: 나만 볼 수 있음 (무료)
- **권장**: **Private** 선택 (보안상 안전)

### 3-4. ⚠️ 중요: 체크박스 확인
다음 항목들은 **체크 해제**해야 합니다:

- ❌ **"Add a README file"** - 체크 해제
- ❌ **"Add .gitignore"** - 체크 해제 (이미 있으므로)
- ❌ **"Choose a license"** - 체크 해제

**왜 체크 해제?**
- 이미 로컬에 코드가 있으므로
- README나 .gitignore를 추가하면 충돌이 발생할 수 있습니다

### 3-5. "Create repository" 버튼 클릭
- 모든 정보 입력 후
- 초록색 **"Create repository"** 버튼을 클릭합니다

---

## 🎯 4단계: 저장소 URL 복사

### 4-1. 저장소 페이지 확인
저장소가 생성되면 다음 페이지가 나타납니다:

```
Quick setup — if you've done this kind of thing before
```

### 4-2. URL 찾기
페이지에 다음과 같은 URL이 보입니다:

```
https://github.com/your-username/cruise-guide.git
```

또는

```
git@github.com:your-username/cruise-guide.git
```

### 4-3. URL 복사
- **HTTPS**로 시작하는 URL을 복사합니다
- 예: `https://github.com/hyeseon28/cruise-guide.git`
- 이 URL을 메모장에 저장해두세요!

---

## 🎯 5단계: 로컬과 GitHub 연결

### 5-1. 터미널 열기
- Cursor에서 터미널을 엽니다
- 또는 명령 프롬프트를 엽니다

### 5-2. 명령어 실행
다음 명령어를 **순서대로** 실행하세요:

```bash
# 1. 프로젝트 폴더로 이동 (이미 있으면 생략)
cd /home/userhyeseon28/projects/cruise-guide

# 2. 기존 연결 제거 (있다면)
git remote remove origin

# 3. GitHub 저장소 연결
# ⚠️ 아래 URL을 4단계에서 복사한 본인의 URL로 변경하세요!
git remote add origin https://github.com/your-username/cruise-guide.git

# 4. 브랜치 이름을 main으로 변경
git branch -M main

# 5. 코드를 GitHub에 푸시
git push -u origin main
```

### 5-3. 인증 요청
`git push` 실행 시 인증을 요구할 수 있습니다:

**방법 1: Personal Access Token 사용 (권장)**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. "Generate new token (classic)" 클릭
3. Note: "Cruise Guide" 입력
4. Expiration: 원하는 기간 선택
5. Scopes: `repo` 체크
6. "Generate token" 클릭
7. **토큰을 복사** (한 번만 보여줌!)
8. 비밀번호 입력 시 이 토큰을 입력

**방법 2: GitHub CLI 사용**
```bash
# GitHub CLI 설치 (없는 경우)
# Ubuntu/WSL:
sudo apt install gh

# 로그인
gh auth login
```

---

## ✅ 6단계: 성공 확인

### 6-1. 터미널 메시지 확인
성공하면 다음과 같은 메시지가 나타납니다:

```
Enumerating objects: X, done.
Counting objects: 100% (X/X), done.
Writing objects: 100% (X/X), done.
To https://github.com/your-username/cruise-guide.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

### 6-2. GitHub 웹사이트에서 확인
1. GitHub 저장소 페이지로 이동
2. 파일 목록이 보이는지 확인
3. `.env` 파일이 **없어야** 정상 (보안상 중요!)

---

## 🆘 문제 해결

### 문제 1: "remote origin already exists" 에러

**해결 방법**:
```bash
git remote remove origin
git remote add origin https://github.com/your-username/cruise-guide.git
```

### 문제 2: 인증 실패

**해결 방법**:
- Personal Access Token 사용 (5-3단계 참고)
- 또는 GitHub CLI 사용

### 문제 3: ".env 파일이 GitHub에 올라갔어요!"

**해결 방법** (중요!):
```bash
# Git에서 .env 제거
git rm --cached .env

# 커밋
git commit -m "Remove .env from Git"

# 푸시
git push

# GitHub에서 .env 파일 삭제 확인
```

---

## 📝 체크리스트

저장소 생성 전:
- [ ] GitHub 계정 있음
- [ ] 로그인 완료

저장소 생성 중:
- [ ] Repository name 입력
- [ ] Public/Private 선택
- [ ] README 체크 해제
- [ ] .gitignore 체크 해제
- [ ] License 체크 해제
- [ ] "Create repository" 클릭

저장소 생성 후:
- [ ] URL 복사 완료
- [ ] 로컬과 연결 완료
- [ ] 코드 푸시 성공
- [ ] GitHub에서 파일 확인

---

**이제 단계별로 진행하세요!** 🚀

**문제가 발생하면 언제든 물어보세요!** 💪










