# 화상 회의 기능 접근 경로

## 📍 위치

### 1. 대리점장 대시보드
**URL**: `/partner/[partnerId]/dashboard`

**예시**:
- `/partner/abc123/dashboard`
- `/partner/boss001/dashboard`

**화면 위치**: 
- 대시보드 상단에 "화상 회의" 섹션이 표시됩니다
- 파란색 배경의 카드 형태로 표시
- "화상 회의" 버튼 클릭

### 2. 관리자 패널
**URL**: `/admin/dashboard`

**화면 위치**:
- 관리자 대시보드 상단에 "화상 회의" 섹션이 표시됩니다
- 파란색 배경의 카드 형태로 표시
- "화상 회의" 버튼 클릭

## 🚀 테스트 방법

### 대리점장으로 테스트

1. **대리점장 로그인**
   ```
   URL: /partner
   또는
   URL: /partner/login
   ```

2. **대시보드 접속**
   ```
   URL: /partner/[대리점장ID]/dashboard
   예: /partner/boss001/dashboard
   ```

3. **화상 회의 버튼 클릭**
   - 대시보드 상단의 "화상 회의" 섹션에서
   - 파란색 "화상 회의" 버튼 클릭

### 관리자로 테스트

1. **관리자 로그인**
   ```
   URL: /admin/login
   ```

2. **대시보드 접속**
   ```
   URL: /admin/dashboard
   ```

3. **화상 회의 버튼 클릭**
   - 대시보드 상단의 "화상 회의" 섹션에서
   - 파란색 "화상 회의" 버튼 클릭

## 📸 화면 구성

### 대리점장 대시보드
```
┌─────────────────────────────────────┐
│  [대리점장 대시보드 헤더]            │
├─────────────────────────────────────┤
│  📊 통계 카드들                      │
├─────────────────────────────────────┤
│  🔗 개인 링크 섹션                   │
├─────────────────────────────────────┤
│  📹 화상 회의                        │  ← 여기!
│  팀원이나 고객과 실시간 화상 통화... │
│  [화상 회의] 버튼                    │
├─────────────────────────────────────┤
│  ⚡ 빠른 메뉴                        │
└─────────────────────────────────────┘
```

### 관리자 패널
```
┌─────────────────────────────────────┐
│  [관리자 대시보드 헤더]              │
├─────────────────────────────────────┤
│  📹 화상 회의                        │  ← 여기!
│  팀원이나 고객과 실시간 화상 통화... │
│  [화상 회의] 버튼                    │
├─────────────────────────────────────┤
│  📊 통계 카드들                      │
└─────────────────────────────────────┘
```

## 🔍 빠른 확인 방법

### 1. 개발 서버 실행
```bash
cd /home/userhyeseon28/projects/cruise-guide
npm run dev
```

### 2. Socket.io 서버 실행 (별도 터미널)
```bash
cd /home/userhyeseon28/projects/cruise-guide
npm run socket
```

### 3. 브라우저에서 접속

**대리점장**:
```
http://localhost:3000/partner/[대리점장ID]/dashboard
```

**관리자**:
```
http://localhost:3000/admin/dashboard
```

## ⚠️ 주의사항

1. **Socket.io 서버 필수**: 화상 회의를 사용하려면 Socket.io 서버가 실행되어야 합니다
2. **두 서버 모두 실행**: Next.js 서버와 Socket.io 서버를 모두 실행해야 합니다
3. **카메라/마이크 권한**: 브라우저에서 카메라와 마이크 접근 권한을 허용해야 합니다

## 🎯 테스트 시나리오

### 시나리오 1: 대리점장이 미팅 생성
1. 대리점장 대시보드 접속
2. "화상 회의" 버튼 클릭
3. "새 미팅 시작하기" 클릭
4. 미팅 정보 입력 후 생성
5. 링크 복사하여 다른 사람에게 공유

### 시나리오 2: 관리자가 미팅 참가
1. 관리자 대시보드 접속
2. "화상 회의" 버튼 클릭
3. 진행 중인 미팅 목록에서 "참가" 클릭
4. 또는 링크로 직접 참가

## 📝 코드 위치

- **대리점장 대시보드**: `app/partner/[partnerId]/dashboard/PartnerDashboard.tsx` (483-495줄)
- **관리자 패널**: `app/admin/dashboard/page.tsx` (323-335줄)
- **화상 회의 위젯**: `components/video/VideoMeetingWidget.tsx`
- **화상 회의 컴포넌트**: `components/video/VideoConference.tsx`
- **Socket.io 서버**: `server/socket-server.js`







