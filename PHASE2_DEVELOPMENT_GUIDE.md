# 🚀 Phase 2 개발 가이드

**시작일**: 2025-11-23  
**현재 버전**: v1.0.0 (main 브랜치)  
**개발 브랜치**: `dev` (또는 `feature/phase2`)

---

## 📋 현재 상태

- ✅ **v1.0.0 배포 완료** (main 브랜치)
- ✅ **v1.0.0 태그 생성 완료**
- ✅ **개발 브랜치 준비 완료** (dev)

---

## 🌿 브랜치 구조

```
main (v1.0.0) ← 운영 중인 안정 버전
  │
  └── dev ← Phase 2 개발 중
       │
       └── feature/새기능 ← 개별 기능 개발 (선택사항)
```

---

## 🎯 Phase 2 개발 워크플로우

### 1. 개발 시작 전 확인

```bash
# dev 브랜치로 전환 확인
git checkout dev

# 최신 상태로 업데이트
git pull origin dev
```

### 2. 기능 개발

```bash
# dev 브랜치에서 직접 개발
# 또는 새 기능 브랜치 생성 (선택사항)
git checkout -b feature/새기능명

# 개발 작업...
git add .
git commit -m "Add: 새 기능 추가"
```

### 3. 커밋 규칙

**커밋 메시지 형식**:
```
Add: 새 기능 추가
Fix: 버그 수정
Update: 기능 개선
Refactor: 코드 리팩토링
```

**예시**:
```bash
git commit -m "Add: Phase 2 - 새로운 기능명"
git commit -m "Fix: Phase 2 - 버그 수정"
git commit -m "Update: Phase 2 - 기능 개선"
```

### 4. 정기적인 백업

```bash
# dev 브랜치 푸시 (정기적으로)
git push origin dev
```

---

## 🔄 main 브랜치와 동기화 (필요시)

### main의 최신 변경사항 가져오기

```bash
# dev 브랜치에서
git checkout dev

# main의 최신 변경사항 가져오기
git fetch origin main

# main의 변경사항을 dev에 병합 (선택사항)
git merge origin/main
```

---

## ✅ Phase 2 완성 후 main 병합

### 1. 최종 테스트

```bash
# dev 브랜치에서
git checkout dev

# 빌드 테스트
npm run build

# 타입 체크
npm run typecheck

# 린트 체크
npm run lint
```

### 2. main에 병합

```bash
# main 브랜치로 전환
git checkout main

# main 최신화
git pull origin main

# dev 브랜치 병합
git merge dev

# 충돌 해결 (있는 경우)
# ... 충돌 해결 ...

# 병합 커밋
git commit -m "Merge: Phase 2 개발 완료"

# main 푸시
git push origin main
```

### 3. 새 버전 태그 생성

```bash
# v2.0.0 태그 생성
git tag -a v2.0.0 -m "Release v2.0.0: Phase 2 완료"

# 태그 푸시
git push origin v2.0.0
```

---

## 🛡️ 안전장치

### 1. main 브랜치 보호

- ✅ main 브랜치는 직접 커밋하지 않음
- ✅ 모든 개발은 dev 브랜치에서 진행
- ✅ main 병합은 테스트 완료 후에만

### 2. 되돌리기 방법

**dev 브랜치 문제 발생 시**:
```bash
# main의 안정 버전으로 되돌리기
git checkout dev
git reset --hard origin/main
```

**또는 v1.0.0으로 되돌리기**:
```bash
git checkout dev
git reset --hard v1.0.0
```

---

## 📝 개발 체크리스트

### 기능 추가 시
- [ ] dev 브랜치에서 작업
- [ ] 커밋 메시지 명확하게 작성
- [ ] 정기적으로 푸시
- [ ] QA 체크리스트 확인

### main 병합 전
- [ ] 모든 기능 테스트 완료
- [ ] 빌드 성공 확인
- [ ] 타입 에러 없음
- [ ] 린트 경고 확인
- [ ] QA 체크리스트 통과

---

## 🎯 현재 작업 환경

**브랜치**: `dev`  
**목적**: Phase 2 개발  
**안정 버전**: `main` (v1.0.0)

---

## 💡 팁

1. **작은 단위로 커밋**: 기능별로 나눠서 커밋
2. **정기적인 푸시**: 하루 작업 끝날 때마다 푸시
3. **명확한 커밋 메시지**: 나중에 이해하기 쉽게
4. **충돌 예방**: main과 정기적으로 동기화

---

**이제 Phase 2 개발을 시작하세요!** 🚀










